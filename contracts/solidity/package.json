{
  "name": "@lastmile-gig/smart-contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Lastmile Gig Smart Contracts - Polygon CDK L2",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "test:coverage": "hardhat coverage",
    "deploy:testnet": "hardhat run scripts/deploy.ts --network polygonCDKTestnet",
    "deploy:mainnet": "hardhat run scripts/deploy.ts --network polygonCDKMainnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.22.0",
    "typescript": "^5.4.0",
    "@types/chai": "^4.3.0",
    "@types/mocha": "^10.0.0"
  }
}
